# Changelog

All notable changes to this project are documented here.

## Unreleased

- Use the pre-built vulnix NVD cache offline by default, retaining
  `VULNIX_MIRROR` as an explicit network fallback.
